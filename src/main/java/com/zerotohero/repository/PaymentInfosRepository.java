package com.zerotohero.repository;

import com.zerotohero.entity.PaymentInfos;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PaymentInfosRepository extends JpaRepository<PaymentInfos, Long> {
}
